language: python
cache:
  pip: true
os: linux
dist: focal
python:
  - "nightly"
install: pip install -U tox-travis
script: tox --skip-missing-interpreters false
after_success:
  - pip install codecov && codecov -X gcov
