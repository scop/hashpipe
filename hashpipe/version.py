"""Hashpipe version."""

# This is a separate file named version.py to match release-please's
# simplest config expectation. Do not rely on its existence, access the
# version constant through `hashpipe.__version__` instead.

__version__ = "0.9.2"
